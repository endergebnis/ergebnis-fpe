# 64-Bit-Server-Secret (einmalig erzeugen mit: ergebnis-fpe init)
SERVER_SECRET=

# Token-Fenster in Minuten (Default: 15). Ein Token ist 3 Fenster lang
# gueltig (MAIN -> SECONDARY -> TERTIARY), danach mathematisch abgelaufen.
WINDOW_MINUTES=15
